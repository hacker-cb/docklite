---
description: Python typing standards with modern syntax (Python 3.8+)
globs: ["**/*.py"]
tags: ["python", "typing"]
alwaysApply: false
---
# Python Typing Standards

## 🎯 Обязательные требования

**ВСЕ функции и методы ДОЛЖНЫ иметь type hints:**
- ✅ Параметры с типами
- ✅ Return type annotations
- ✅ Для void функций используйте `-> None:`

**НЕТ ИСКЛЮЧЕНИЙ** - typing обязателен для всего нового кода.

## 📦 Современный синтаксис (Python 3.8+ с annotations)

### 1. Использование `from __future__ import annotations`

**ВСЕГДА** добавляйте в начало файла:

```python
from __future__ import annotations

from typing import Optional, Literal
```

**Почему:** Позволяет использовать современный синтаксис в Python 3.8+

### 2. Встроенные типы (list, dict, tuple)

**С `from __future__ import annotations` используйте lowercase:**

```python
from __future__ import annotations

# ✅ ПРАВИЛЬНО (современный синтаксис)
def get_items() -> list[str]:
    return ["item1", "item2"]

def get_mapping() -> dict[str, int]:
    return {"a": 1, "b": 2}

def get_pair() -> tuple[str, int]:
    return ("name", 42)

# ❌ НЕПРАВИЛЬНО (устаревший синтаксис)
from typing import List, Dict, Tuple

def get_items() -> List[str]:  # Используйте list[str]
    ...
```

### 3. Optional и None

**Используйте `Optional` для опциональных значений:**

```python
from typing import Optional

# ✅ ПРАВИЛЬНО
def find_user(user_id: int) -> Optional[User]:
    return user or None

def process(value: Optional[str] = None) -> str:
    return value or "default"

# ❌ НЕПРАВИЛЬНО
def find_user(user_id: int) -> User | None:  # Используйте Optional
    ...
```

### 4. Literal для строгих значений

**Используйте `Literal` для ограниченного набора значений:**

```python
from typing import Literal

# ✅ ПРАВИЛЬНО - IDE и mypy проверят корректность
Operation = Literal["start", "stop", "restart"]

def control_container(operation: Operation) -> None:
    ...  # Только "start", "stop", "restart" разрешены

# ❌ НЕПРАВИЛЬНО
def control_container(operation: str) -> None:
    ...  # Любая строка разрешена
```

**Определения типов:** `backend/app/types.py`

```python
ContainerOperation = Literal["start", "stop", "restart", "remove"]
ContainerState = Literal["running", "exited", "paused"]
ProjectStatus = Literal["created", "deployed", "error"]
```

### 5. Async функции и генераторы

```python
from typing import AsyncGenerator

# Async generator
async def stream_data() -> AsyncGenerator[str, None]:
    for item in data:
        yield item

# Regular generator
def iter_items() -> Generator[int, None, None]:
    for i in range(10):
        yield i
```

## 📝 Стандарты по областям

### Backend Services

```python
from __future__ import annotations

from typing import Optional

class ProjectService:
    def __init__(self, db: AsyncSession) -> None:
        self.db = db
    
    async def create_project(
        self, 
        data: ProjectCreate, 
        owner_id: int
    ) -> tuple[Optional[Project], Optional[str]]:
        """
        Create project.
        
        Returns:
            Tuple of (project, error_message)
        """
        ...
```

**Паттерн:** Возвращаем `tuple[Optional[T], Optional[str]]` для операций с ошибками.

### Backend API Endpoints

```python
from __future__ import annotations

from fastapi import APIRouter

router = APIRouter()

@router.get("/users", response_model=list[UserResponse])
async def get_users(
    db: AsyncSession = Depends(get_db),
    current_user: User = Depends(get_current_active_user)
) -> list[dict]:
    """Get all users."""
    ...
    return users  # FastAPI автоматически валидирует response_model

@router.post("/users", status_code=201)
async def create_user(
    user_data: UserCreate,
    db: AsyncSession = Depends(get_db)
) -> dict:
    ...
```

**Правило:** FastAPI endpoints должны иметь `response_model` И return type hint.

### CLI Utilities

```python
from __future__ import annotations

from typing import Optional

def log_info(message: str) -> None:
    """Print info message."""
    console.print(f"ℹ {message}", style="blue")

def get_hostname() -> str:
    """Get server hostname."""
    return hostname or "localhost"

def get_access_url(
    path: str = "", 
    port: Optional[str] = None
) -> str:
    """Build access URL."""
    ...
```

**Правило:** Все utility функции с типами, даже простые.

### Test Fixtures

```python
from __future__ import annotations

import pytest
from typing import AsyncGenerator, Generator

@pytest.fixture
async def db_session() -> AsyncGenerator[AsyncSession, None]:
    """Create test database session."""
    async with TestSessionLocal() as session:
        yield session

@pytest.fixture
def sample_data() -> dict:
    """Return sample test data."""
    return {"key": "value"}

@pytest.fixture
def temp_dir() -> Generator[str, None, None]:
    """Create temporary directory."""
    temp = tempfile.mkdtemp()
    yield temp
    shutil.rmtree(temp)
```

**Правило:** Fixtures ДОЛЖНЫ иметь return type hints для IDE поддержки.

## 🔍 mypy - Статическая проверка

### Конфигурация

**Backend:** `backend/mypy.ini`  
**CLI:** `scripts/mypy.ini`

### Запуск проверки

```bash
# Backend
cd backend
mypy app tests

# CLI
cd scripts
mypy cli
```

### Игнорирование ошибок

**Используйте комментарии только при необходимости:**

```python
# Для конкретной строки
result = some_untyped_lib()  # type: ignore

# Для конкретной ошибки
result = some_func()  # type: ignore[attr-defined]

# Для всего файла (только для миграций/алембик)
# mypy: ignore-errors
```

**НЕ ЗЛОУПОТРЕБЛЯЙТЕ** `type: ignore` - старайтесь исправить типы.

## 📋 Чеклист для новых файлов

- [ ] Добавлен `from __future__ import annotations` в начале
- [ ] Все функции имеют type hints
- [ ] Используется `list/dict/tuple` вместо `List/Dict/Tuple`
- [ ] `Optional` для опциональных значений
- [ ] `Literal` для фиксированных значений где возможно
- [ ] Async generators типизированы как `AsyncGenerator[T, None]`
- [ ] Void функции имеют `-> None:`
- [ ] Docstrings описывают параметры и возвращаемые значения
- [ ] mypy проверка проходит без ошибок

## ❌ Типичные ошибки

### 1. Забыли from __future__

```python
# ❌ НЕПРАВИЛЬНО
def get_items() -> list[str]:  # SyntaxError в Python 3.8!
    ...

# ✅ ПРАВИЛЬНО
from __future__ import annotations

def get_items() -> list[str]:  # Работает в Python 3.8+
    ...
```

### 2. Использование старого синтаксиса

```python
from typing import List, Dict  # Не нужно с annotations!

# ❌ НЕПРАВИЛЬНО
def process(items: List[str]) -> Dict[str, int]:
    ...

# ✅ ПРАВИЛЬНО
def process(items: list[str]) -> dict[str, int]:
    ...
```

### 3. Отсутствие return type

```python
# ❌ НЕПРАВИЛЬНО
def check_admin(user: User):  # Нет return type!
    if not user.is_admin:
        raise HTTPException(...)

# ✅ ПРАВИЛЬНО
def check_admin(user: User) -> None:  # Явно указан None
    if not user.is_admin:
        raise HTTPException(...)
```

### 4. Неправильное использование Literal

```python
# ❌ НЕПРАВИЛЬНО - runtime значение
Operation = Literal["start"]  # Только для типов!
operation = Operation  # Ошибка!

# ✅ ПРАВИЛЬНО - type alias
Operation = Literal["start", "stop"]
def do(op: Operation) -> None:  # Используем как тип
    ...
```

## 🎓 Полезные ссылки

- [Python typing docs](https://docs.python.org/3/library/typing.html)
- [mypy documentation](https://mypy.readthedocs.io/)
- [PEP 585 - Type Hinting Generics](https://peps.python.org/pep-0585/)
- [PEP 563 - Postponed Evaluation](https://peps.python.org/pep-0563/)

## 📊 Статус проекта

**✅ Покрытие typing:** 95%+  
**✅ mypy конфигурация:** Настроен  
**✅ CI/CD проверка:** Добавлена  
**✅ Документация:** Актуальна

---

**ПОМНИТЕ:** Type hints делают код самодокументируемым, предотвращают ошибки и улучшают поддержку IDE!
